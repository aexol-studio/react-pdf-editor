import { style } from "typestyle";

export const Wrap = style({
  display: "flex",
  flexFlow: "column nowrap"
});
export const Main = style({
  width: 30,
  height: "auto",
  marginRight: 10,
  maxWidth: 100
});
